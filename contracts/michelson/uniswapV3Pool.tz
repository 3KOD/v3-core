# None
{
  storage (option address);
  parameter (address %testfunc);
  code { UNPAIR;
         DUP;
         SOME;
         DIP { DIG 1; DROP };
         DUG 1;
         DROP;
         NIL operation;
         PAIR };
}
